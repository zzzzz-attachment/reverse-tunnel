services:
  - type: web
    name: status-api
    runtime: python
    plan: free
    buildCommand: pip install -r relay/requirements.txt
    startCommand: python relay/relay.py
    envVars:
      - key: TUNNEL_TOKEN
        generateValue: true   # Render generates a strong token; copy it from the dashboard
